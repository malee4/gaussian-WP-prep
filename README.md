# Wavepacket Preparation Methods

This repository contains sample code for W state preparation and computation of coefficients for wavepacket construction.
To set up the environment to run the notebooks using Conda, enter the `gaussian-WP-prep` repository via your terminal and run
```bash
conda env create -f environment.yml
```